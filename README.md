## Word 课程信息解析器
```bash
# 安装依赖
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt

# 启动项目：
source venv/bin/activate # Linux
./venv/bin/activate # Windows
streamlit run src/app.py --server.port INTEGER
```
